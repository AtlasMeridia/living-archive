[project]
name = "living-archive"
version = "0.1.0"
description = "AI-assisted family photo archive pipeline"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.42",
    "httpx>=0.27",
    "Pillow>=10.0",
    "python-dotenv>=1.0",
]

[project.scripts]
run-slice = "src.run_slice:main"
