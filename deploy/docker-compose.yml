services:
  dashboard:
    build: .
    container_name: la-dashboard
    restart: unless-stopped
    network_mode: host
    volumes:
      - /home/atlas/living-archive:/app:ro
      - /home/atlas/living-archive-data:/data:ro
    env_file: .env
